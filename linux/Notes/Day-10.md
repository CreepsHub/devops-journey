## What is the difference between cat and less?
cat reads the whole content of a file , less reads files but in pages one after the other .
## Why would a DevOps engineer use tail -f?
in a scenario , where new data or files are to be observed , tail -f could be used .
{It monitors a file in real time as new lines are added.}
## What information does wc provide?
This provides the word count a a file using the arrangment, lines , word , character .
## What does grep do?
grep searches out a text inside a file .
## Which command did you find most useful today, and why?
its less - lately reading out file output has looked bulky and it always scrolls its way down to the end when i'm using cat , less has been very helpful in instances where i need to view large files .