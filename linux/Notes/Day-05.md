## 1. Why does Linux use file permissions?
so that files have authorized access to some individuals only
## 2. What is the difference between Read, Write and Execute?
write is used to alter/edit the content of the file , while execute is used to script .
## 3. Why shouldn't every user have full permissions?
Thats because some users need to be restricted due to private policies .
## 4. Which command changes permissions?
chmod
## 5. Which command displays permissions?
ls -l